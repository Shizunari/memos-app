<header>
  <div class="twin-header">
    <h1>Memoアプリ</h1>
    <a class="add-btn" href="/memos/new">新規追加</a>
  </div>
</header>

<main>
  <div class="index-article">
  <% @articles.each do |article| %>
    <p><a href="/memos/<%= article["memo_id"] %>">タイトル: <%= article["title"] %></a></p>
  <% end %>
  </div>
</main>
